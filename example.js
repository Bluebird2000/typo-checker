// Example file with some intentional typos
const message = "Welcom to Lagos!";
const colors = {
  primary: "#ff0000",
  secondary: "#00ff00",
  text: "#000000",
};

const states = ["Lagos", "Abuja", "Kano", "Port Harcourt"];

const config = {
  apiUrl: "https://api.example.com",
  timeout: 5000,
  retryCount: 3,
};

// Some more typos
const welcomeMessage = "Hellow wrld!";
const errorMessage = "An errr occured";
